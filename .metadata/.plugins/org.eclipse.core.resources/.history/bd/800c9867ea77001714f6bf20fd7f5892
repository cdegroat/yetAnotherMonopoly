package game;

import java.math.BigDecimal;

public class LuxuryTaxSquare extends MonopolySquare {
	
	public LuxuryTaxSquare(){
		super.setName("Luxury Tax");
		super.setOwnable(false);
	}
	
	@Override
	public int getChargeAmount(IMonopolyPlayer player, DiceResult diceResult) {
		BigDecimal netWorth = new BigDecimal(player.getNetWorth());
		BigDecimal netWorthCalc = netWorth.divide(new BigDecimal(10),0,BigDecimal.ROUND_HALF_UP);
		if(netWorthCalc.compareTo(new BigDecimal(900)) < 0){
			return netWorthCalc.intValue();
		}
		return 900;
	}

}
